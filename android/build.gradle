allprojects {
    repositories {
        google()
        mavenCentral()
        maven {
            url = uri("${project(':app').projectDir}/repo")
        }
    }
}

tasks.register("clean", Delete) {
    delete rootProject.buildDir
}
